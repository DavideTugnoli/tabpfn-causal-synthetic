# CTGAN Custom SCM Results

This final lightweight bundle contains the 500 validated CTGAN result rows for
Custom SCM: 100 paired seeds at each training size in
`20,50,100,200,500`.

The seed sets exactly match the canonical cleaned TabPFN Custom SCM seed sets.
The public CSV excludes machine-specific absolute paths; raw runs and synthetic
NPZ files remain outside Git.
