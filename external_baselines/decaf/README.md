# DECAF external baseline

This directory integrates the official DECAF repository with the shared
paired-cleaned-seed external baseline protocol. It is isolated from the TabPFN
environment and writes models, synthetic datasets, metrics, and logs to a
dedicated run root.

See `DEFAULTS.md` before interpreting results.

